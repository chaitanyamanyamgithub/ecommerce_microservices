apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ecommerce

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: k6-flash-sale-script
    files:
      - scripts/flash-sale-product-browse.js

resources:
  - k6-flash-sale-demo-job.yaml
